# 💰 Bank Management System (C Project)

A **Bank Management System** built entirely in **C language**, featuring a **modern console-based frontend** (green theme), user account handling, secure transactions, and an admin loan approval system.  
This project simulates core banking operations — account creation, balance management, money transfer, and transaction tracking — using file handling and linked lists.

---

## 🖥️ Features

### 👤 User Features
- Create a new bank account with personal details  
- Login securely with password masking  
- Check account details and balance  
- Deposit and withdraw money  
- Transfer funds to another account  
- View complete transaction history  
- Apply for a loan (awaiting admin approval)  
- Log out safely without data loss  

### 🧑‍💼 Admin Features
- Secure admin login (`admin / admin123`)  
- View all registered users and balances  
- Approve or reject loan requests  
- Manage loan disbursements automatically  

---

## 🎨 Console Frontend (UI)
The program uses a **colorful console interface** for a better user experience:
- Green-themed UI for a banking look  
- Clear menu navigation with headers  
- Smooth screen transitions using `system("cls")`  
- Password input hidden with `*` masking  

---

## ⚙️ Technologies Used
- **Language:** C (ANSI C)
- **Compiler:** GCC / Turbo C / Code::Blocks
- **Libraries:**  
  - `<stdio.h>` for input/output  
  - `<stdlib.h>` for memory handling  
  - `<string.h>` for string operations  
  - `<conio.h>` for password masking  
  - `<windows.h>` for colored text and delays  

---

